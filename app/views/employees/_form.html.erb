<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fname %><br />
    <%= f.text_field :fname %>
  </div>
  <div class="field">
    <%= f.label :lname %><br />
    <%= f.text_field :lname %>
  </div>
  <%= f.fields_for :address do |a| %>
    <div class="actions">  
    	<%= a.label :street %><br />
      <%= a.text_field :street %>
    </div>
    <div class="actions">  
    	<%= a.label :city %><br />
      <%= a.text_field :city %>
    </div>
    <div class="actions">  
    	<%= a.label :street %><br />
      <%= a.text_field :street %>
    </div>
    <div class="actions">  
    	<%= a.label :city %><br />
      <%= a.text_field :city %>
    </div>  
  <% end %>
  
  <% @employee.projects.each_with_index do |project, index| %>
    <%= f.fields_for "projects[]", project do |p| %>
      <div class="actions">  
    	  <%= p.label :name %><br />
        <%= p.text_field :name %>
      </div>
      <div class="actions">  
    	  <%= p.label :manager %><br />
        <%= p.text_field :manager %>
      </div>
      <div class="actions">  
    	  <%= p.label :name %><br />
        <%= p.text_field :name %>
      </div>
      <div class="actions">  
    	  <%= p.label :manager %><br />
        <%= p.text_field :manager %>
      </div>  
    <% end %>
  <% end %>
    
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
